version: '3.6'

services:
  web:
    image: nginx:1.19.3-alpine
    ports:
      - 5000:80
    volumes:
      - ./public:/usr/share/nginx/html:ro
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.vblinden.rule=Host(`vblinden.xyz`)'
